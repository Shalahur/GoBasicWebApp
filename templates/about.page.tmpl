{{template "base"}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>this is about page.</h1>
               <p>This come from the handler: {{index .StringMap "test"}}</p>
            </div>
        </div>
    </div>
{{end}}